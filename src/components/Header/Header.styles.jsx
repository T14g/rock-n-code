import styled from 'styled-components';

const HeaderStyles = styled.div`
    display: flex;

    ul {
        li {
            display: inline;
            margin-right: 10px;
        }
    }
`;

export default HeaderStyles